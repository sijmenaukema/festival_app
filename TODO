create spring initializr V
create repo bitbucket V
create classes for DJ V
create classes for set V
create Restcontroller(s) V
CRUD into JPA H2 V
many/one many V
controller => antwoordmodel; V
testing:
review application:
docker:

field list van MusicSets V
    SQL custom query V
    => postgressDB V

Controller validation and error handling V

update -> id(not found) invalid ID exception; V

runtime exception:
    * folder maken voor logische exceptions
    e.a invalid ID name exception
    Exception met class name super() Runtime exception

Part: test maken;
    regressie test vs unit test;

project hosten via een localhost container

front end;

DTO: class aanmaken met ID
* Docker voor festival_app V

* JAR lokaal builden ; V
* image in container doen ; V
* container spinnen ; V
* Postgres;
* mongo local mount; command!?
* Docker compose ++
* Docker network;